name: admin/web CI

on:
  push:
    branches: [main]
    paths:
      - "Client/admin/web/**"
      - "infra/docker/admin-web/**"
      - ".github/workflows/admin-web-ci.yml"
  pull_request:
    paths:
      - "Client/admin/web/**"
      - "infra/docker/admin-web/**"
      - ".github/workflows/admin-web-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Client/admin/web
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: Client/admin/web/package-lock.json

      - run: npm ci
      - run: npm run lint
      - run: npm run build
